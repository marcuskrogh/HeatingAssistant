# Sandbox: compute KPI loading overlay

Serve a representative pair of room-view compute gauges.

```bash
python3 sandbox/kpi-loading/harness.py --tag 01
python3 sandbox/kpi-loading/harness.py --serve-only   # then open the printed URL
```
