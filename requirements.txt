# Runtime dependencies for the HeatingAssistant custom component.
#
# The mbc (model-based control) package provides the generic nonlinear
# model abstractions, state estimation (CD-EKF), and optimal control
# algorithms (CDTrackingOptimalControlProblem, CDNMPCController).
#
# The `[ipopt]` extra pulls in cyipopt — the binding mbc's IpoptNLPBackend
# uses for both MPC and parameter estimation.  Installing cyipopt requires
# the IPOPT system library (libipopt) and its development headers to be
# available to pkg-config at build time.
#
numpy
scipy
cvxopt
mbc[ipopt] @ git+https://github.com/marcuskrogh/mbc.git@a472a841c09951b5c460aa1f6ed38d1bb442b5a9
