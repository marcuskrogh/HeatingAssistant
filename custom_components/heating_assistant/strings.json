{
  "config": {
    "step": {
      "user": {
        "title": "Heating Assistant",
        "description": "Connect this Home Assistant instance to the HeatingAssistant App MQTT bridge.",
        "data": {
          "instance_id": "App instance ID"
        },
        "data_description": {
          "instance_id": "MQTT instance id used by the App. Leave as default unless you configured a different App instance."
        }
      }
    },
    "abort": {
      "already_configured": "This Heating Assistant App instance is already configured."
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart required",
      "description": "HeatingAssistant installed a new thin integration ({version}). Restart Home Assistant to load it.",
      "fix_flow": {
        "step": {
          "confirm_restart": {
            "title": "Restart Home Assistant",
            "description": "Restart now to load the synced {name} integration."
          }
        }
      }
    }
  }
}
