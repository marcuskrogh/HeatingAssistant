# Development and test dependencies for Heating Assistant.
# Runtime deps are in requirements.txt / manifest.json.
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
PyYAML>=6.0

# Home Assistant is only needed for type-checking imports in some IDE setups;
# tests run against stubs in tests/conftest.py.
# homeassistant>=2024.1
