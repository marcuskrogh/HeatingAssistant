name: "HeatingAssistant"
version: "2.0.6"
slug: heatingassistant
description: "Heating control App for Home Assistant OS (SWD-255)"
url: "https://github.com/marcuskrogh/HeatingAssistant"
arch:
  - amd64
  - aarch64
init: false
startup: application
boot: auto
timeout: 120
watchdog: "http://[HOST]:[PORT:8100]/"
ingress: true
ingress_port: 8100
panel_icon: mdi:radiator
panel_title: HeatingAssistant
ports:
  8100/tcp: 8100
map:
  - type: data
    read_only: false
  - type: homeassistant_config
    read_only: false
options:
  instance_id: default
  mqtt_broker: core-mosquitto
  mqtt_port: 1883
  mqtt_username: ""
  mqtt_password: ""
schema:
  instance_id: str
  mqtt_broker: str
  mqtt_port: port
  mqtt_username: str
  mqtt_password: password
